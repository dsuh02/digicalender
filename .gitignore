# Runtime database (SQLite + WAL sidecars)
digicalender.db
digicalender.db-wal
digicalender.db-shm
*.db
*.db-wal
*.db-shm

# Real credentials — only config.example.json is tracked
config.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
# Uploaded photos: one folder per gallery set. Yours, not the app's.
galleries/

# Host-specific notes: addresses, users, device IDs, quirks of one house.
# Everything in here is deliberately NOT public.
LOCAL.md
